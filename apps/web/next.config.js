/** @type {import('next').NextConfig} */
const nextConfig = {
  // Makes `next build` output a standalone app. For more, consult
  // https://github.com/vercel/next.js/tree/canary/examples/with-docker#in-existing-projects
  output: "standalone",
};

export default nextConfig;
